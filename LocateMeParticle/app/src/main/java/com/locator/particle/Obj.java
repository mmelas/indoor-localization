package com.locator.particle;

public class Obj extends Rectangle {

    //    ((x,y), (x,y), (x, y), (x, y))
    public Obj(double coords[][], boolean isNearWindow) {
        super(coords);
    }

}
